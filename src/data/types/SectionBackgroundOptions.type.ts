// Objetivo: Limitar as opções de cores para o background de cada seção.

// Cada seção deve ter uma cor de background diferente. Para isso, criamos
// um tipo que limita as opções de cores para o background de cada seção.

export type SectionBackgroundOptions =
  | 'bg-primary-gray'
  | 'bg-primary-yellow'
  | 'bg-primary-red';
